apiVersion: v1
kind: Service
metadata:
  name: {{ template "zammad.fullname" . }}
  labels:
    app: {{ template "zammad.name" . }}
    chart: {{ template "zammad.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.zammad.service.type }}
  ports:
    - port: {{ .Values.zammad.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "zammad.name" . }}
    release: {{ .Release.Name }}
